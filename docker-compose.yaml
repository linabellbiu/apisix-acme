version: '3.8'

services:
  apisix-acme:
    build: .
    container_name: apisix-acme
    restart: always
    volumes:
      - ./config.yaml:/root/data/config.yaml
      - ./data:/root/data
    networks:
      - apisix

networks:
  apisix:
    external: true
    name: apisix_apisix
